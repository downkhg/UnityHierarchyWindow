using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SphereScprit : MonoBehaviour
{
    public GameObject m_objCube;
    [SerializeField]
    CubeScprit m_cCube;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
